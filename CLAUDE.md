# CLAUDE.md

After completing any task in this repository, run `git add`, commit
with a clear message, and `git push`, without asking for confirmation
— unless the user explicitly says otherwise for that task.
